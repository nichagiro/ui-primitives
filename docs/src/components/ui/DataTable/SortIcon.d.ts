import { SortDirection } from './types';
type SortIconProps = {
    active: boolean;
    direction: SortDirection;
};
export declare function SortIcon({ active, direction }: SortIconProps): import("react").JSX.Element;
export {};
