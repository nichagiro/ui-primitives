import { DataTableProps } from './types';
export declare function DataTable<T extends Record<string, unknown>>({ columns, data, keyExtractor, className, colorScheme: outerColorScheme, pageSize: defaultPageSize, searchable, showPageSize: showPageSizeProp, searchPlaceholder, selection, selected: controlledSelected, onSelectionChange, card, loading, density, stickyFirst, striped, scrollable, emptyContent, onRowClick, toolbarActions, renderExpanded, expanded: controlledExpanded, onExpandedChange, }: DataTableProps<T>): import("react").JSX.Element;
