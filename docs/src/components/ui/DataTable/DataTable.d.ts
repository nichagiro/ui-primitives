import { DataTableProps } from './types';
export declare function DataTable<T extends Record<string, unknown>>({ columns, data, keyExtractor, className, colorScheme: outerColorScheme, pageSize: defaultPageSize, searchable, showPageSize: showPageSizeProp, searchPlaceholder, selection, selected: controlledSelected, onSelectionChange, loading, density, stickyFirst, striped, scrollable, emptyContent, onRowClick, renderExpanded, expandOnRowClick, rowClassName, expanded: controlledExpanded, onExpandedChange, editTrigger, onCellEdit, rowHeight: outerRowHeight, }: DataTableProps<T>): import("react").JSX.Element;
